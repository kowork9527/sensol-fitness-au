{%- liquid
  assign p = product
-%}
{%- style -%}
  .section-{{ section.id }}-padding {
    padding-top: {{ section.settings.padding_top }}px;
    padding-bottom: {{ section.settings.padding_bottom | default: 80 }}px;
    padding-left: 20px;
    padding-right: 20px;
    background: #F7F4F0;
    box-sizing: border-box;
  }
  .section-{{ section.id }}-padding *,
  .section-{{ section.id }}-padding *::before,
  .section-{{ section.id }}-padding *::after {
    box-sizing: border-box;
  }
  /* Breadcrumb */
  .flexair-breadcrumb-{{ section.id }} {
    max-width: 1200px;
    margin: 0 auto 24px;
    font-size: 12px;
    color: #8A8279;
    letter-spacing: 0.05em;
  }
  .flexair-breadcrumb-{{ section.id }} a {
    color: #8A8279;
    text-decoration: none;
    transition: color 0.2s;
  }
  .flexair-breadcrumb-{{ section.id }} a:hover {
    color: #1A1816;
  }
  .flexair-breadcrumb-{{ section.id }} span {
    margin: 0 8px;
  }
  .flexair-product-{{ section.id }} {
    max-width: 1200px;
    margin: 0 auto;
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 60px;
    align-items: start;
    font-family: 'Inter', -apple-system, BlinkMacSystemFont, sans-serif;
    color: #1A1816;
    min-width: 0;
  }
  .flexair-gallery-{{ section.id }} {
    position: sticky;
    top: 20px;
    min-width: 0;
    max-width: 100%;
  }
  .flexair-gallery-main-{{ section.id }} {
    aspect-ratio: 1 / 1;
    background: #fff;
    overflow: hidden;
    margin-bottom: 16px;
    cursor: zoom-in;
    position: relative;
    min-width: 0;
    max-width: 100%;
  }
  .flexair-gallery-main-{{ section.id }} img {
    width: 100% !important;
    height: 100% !important;
    max-width: 100% !important;
    object-fit: cover;
    display: block;
    transition: transform 0.3s;
  }
  .flexair-gallery-main-{{ section.id }}:hover img {
    transform: scale(1.02);
  }
  .flexair-gallery-thumbs-{{ section.id }} {
    display: flex;
    gap: 10px;
    overflow-x: auto;
    scroll-behavior: smooth;
    -webkit-overflow-scrolling: touch;
    padding-bottom: 6px;
  }
  .flexair-gallery-thumbs-{{ section.id }}::-webkit-scrollbar {
    height: 4px;
  }
  .flexair-gallery-thumbs-{{ section.id }}::-webkit-scrollbar-track {
    background: transparent;
  }
  .flexair-gallery-thumbs-{{ section.id }}::-webkit-scrollbar-thumb {
    background: rgba(0,0,0,0.15);
    border-radius: 2px;
  }
  .flexair-gallery-thumb-{{ section.id }} {
    flex: 0 0 72px;
    width: 72px;
    height: 72px;
    background: #fff;
    overflow: hidden;
    cursor: pointer;
    border: 2px solid transparent;
    border-radius: 6px;
    transition: border-color 0.2s;
  }
  .flexair-gallery-thumb-{{ section.id }}:hover,
  .flexair-gallery-thumb-{{ section.id }}.active {
    border-color: #1A1816;
  }
  .flexair-gallery-thumb-{{ section.id }} img {
    width: 100% !important;
    height: 100% !important;
    max-width: 100% !important;
    object-fit: cover;
    display: block;
  }
  .flexair-gallery-main-wrapper-{{ section.id }} {
    position: relative;
    margin-bottom: 16px;
    min-width: 0;
    max-width: 100%;
  }
  .flexair-gallery-nav-{{ section.id }} {
    position: absolute;
    top: 50%;
    transform: translateY(-50%);
    width: 48px;
    height: 48px;
    background: rgba(255,255,255,0.92);
    border: none;
    border-radius: 50%;
    font-size: 28px;
    line-height: 1;
    cursor: pointer;
    display: flex;
    align-items: center;
    justify-content: center;
    z-index: 10;
    opacity: 0.65;
    transition: opacity 0.3s, background 0.2s;
    color: #1A1816;
  }
  .flexair-gallery-main-wrapper-{{ section.id }}:hover .flexair-gallery-nav-{{ section.id }} {
    opacity: 1;
  }
  .flexair-gallery-nav-{{ section.id }}:hover {
    background: rgba(255,255,255,1);
  }
  .flexair-gallery-nav-prev-{{ section.id }} { left: 12px; }
  .flexair-gallery-nav-next-{{ section.id }} { right: 12px; }

  .flexair-lightbox-nav-{{ section.id }} {
    position: absolute;
    top: 50%;
    transform: translateY(-50%);
    width: 56px;
    height: 56px;
    background: rgba(255,255,255,0.15);
    border: 1px solid rgba(255,255,255,0.3);
    border-radius: 50%;
    color: #fff;
    font-size: 32px;
    cursor: pointer;
    display: flex;
    align-items: center;
    justify-content: center;
    z-index: 10001;
    transition: background 0.2s;
  }
  .flexair-lightbox-nav-{{ section.id }}:hover {
    background: rgba(255,255,255,0.3);
  }
  .flexair-lightbox-nav-prev-{{ section.id }} { left: 30px; }
  .flexair-lightbox-nav-next-{{ section.id }} { right: 30px; }
  .flexair-info-{{ section.id }} {
    padding: 20px 0;
    min-width: 0;
  }
  .flexair-info-brand-{{ section.id }} {
    font-size: 11px;
    font-weight: 500;
    letter-spacing: 0.2em;
    text-transform: uppercase;
    color: #8A8279;
    margin-bottom: 12px;
  }
  .flexair-info-title-{{ section.id }} {
    font-size: 36px;
    font-weight: 300;
    letter-spacing: 0.04em;
    margin: 0 0 8px;
    color: #1A1816;
  }
  .flexair-info-subtitle-{{ section.id }} {
    font-size: 16px;
    color: #8A8279;
    margin-bottom: 16px;
  }
  .flexair-info-reviews-{{ section.id }} {
    display: flex;
    align-items: center;
    gap: 8px;
    margin-bottom: 16px;
    font-size: 13px;
    color: #8A8279;
  }
  .flexair-info-stars-{{ section.id }} {
    color: #F5A623;
    letter-spacing: 2px;
  }
  .flexair-info-model-{{ section.id }} {
    font-size: 12px;
    color: #8A8279;
    letter-spacing: 0.05em;
    margin-bottom: 24px;
    padding-bottom: 24px;
    border-bottom: 1px solid #D4CEC6;
  }
  .flexair-info-price-{{ section.id }} {
    font-size: 28px;
    font-weight: 400;
    color: #1A1816;
    margin-bottom: 8px;
    letter-spacing: 0.02em;
  }
  .flexair-info-price-{{ section.id }} s {
    color: #8A8279;
    margin-right: 12px;
    font-size: 18px;
  }
  .flexair-info-afterpay-{{ section.id }} {
    font-size: 13px;
    color: #8A8279;
    margin-bottom: 24px;
  }
  .flexair-info-badges-{{ section.id }} {
    display: flex;
    gap: 16px;
    flex-wrap: wrap;
    font-size: 11px;
    letter-spacing: 0.1em;
    text-transform: uppercase;
    color: #8A8279;
    margin-bottom: 24px;
    padding-bottom: 24px;
    border-bottom: 1px solid #D4CEC6;
  }
  .flexair-info-stock-{{ section.id }} {
    font-size: 12px;
    letter-spacing: 0.15em;
    text-transform: uppercase;
    margin-bottom: 24px;
    padding: 8px 16px;
    display: inline-block;
  }
  .flexair-info-stock-in-{{ section.id }} {
    background: #E8F5E9;
    color: #2E7D32;
  }
  .flexair-info-stock-out-{{ section.id }} {
    background: #FFEBEE;
    color: #C62828;
  }
  .flexair-qty-{{ section.id }} {
    display: flex;
    align-items: center;
    gap: 16px;
    margin-bottom: 16px;
  }
  .flexair-qty-label-{{ section.id }} {
    font-size: 12px;
    letter-spacing: 0.15em;
    text-transform: uppercase;
    color: #8A8279;
  }
  .flexair-qty-input-{{ section.id }} {
    width: 80px;
    padding: 12px;
    border: 1px solid #D4CEC6;
    background: #fff;
    font-size: 16px;
    text-align: center;
  }
  .flexair-add-{{ section.id }} {
    width: 100%;
    padding: 20px;
    background: #1A1816;
    color: #F7F4F0;
    border: none;
    font-size: 14px;
    letter-spacing: 0.2em;
    text-transform: uppercase;
    cursor: pointer;
    transition: background 0.3s;
    font-weight: 500;
    margin-bottom: 12px;
  }
  .flexair-add-{{ section.id }}:hover {
    background: #2A2826;
  }
  .flexair-add-{{ section.id }}:disabled {
    background: #8A8279;
    cursor: not-allowed;
  }
  .flexair-payment-buttons-{{ section.id }} {
    width: 100%;
    margin-bottom: 32px;
  }
  .flexair-payment-buttons-{{ section.id }} .shopify-payment-button {
    width: 100%;
  }
  .flexair-payment-buttons-{{ section.id }} .shopify-payment-button__button {
    width: 100%;
    padding: 16px;
    background: #fff;
    border: 1px solid #D4CEC6;
    font-size: 12px;
    letter-spacing: 0.1em;
    text-transform: uppercase;
    color: #8A8279;
    text-align: center;
  }
  .flexair-features-{{ section.id }} {
    list-style: none;
    padding: 0;
    margin: 0;
  }
  .flexair-features-{{ section.id }} li {
    font-size: 14px;
    line-height: 1.6;
    color: #1A1816;
    padding: 8px 0;
    padding-left: 20px;
    position: relative;
  }
  .flexair-features-{{ section.id }} li::before {
    content: '•';
    position: absolute;
    left: 0;
    color: #8A8279;
  }
  /* Lightbox */
  .flexair-lightbox-{{ section.id }} {
    display: none;
    position: fixed;
    inset: 0;
    background: rgba(0,0,0,0.95);
    z-index: 9999;
    align-items: center;
    justify-content: center;
    cursor: zoom-out;
  }
  .flexair-lightbox-{{ section.id }}.active {
    display: flex;
  }
  .flexair-lightbox-{{ section.id }} img {
    max-width: 90vw;
    max-height: 90vh;
    object-fit: contain;
  }
  .flexair-lightbox-close-{{ section.id }} {
    position: absolute;
    top: 20px;
    right: 20px;
    width: 40px;
    height: 40px;
    background: none;
    border: 1px solid #fff;
    color: #fff;
    font-size: 24px;
    cursor: pointer;
    display: flex;
    align-items: center;
    justify-content: center;
  }
  @media screen and (max-width: 990px) {
    .flexair-product-{{ section.id }} {
      grid-template-columns: 1fr;
      gap: 40px;
    }
    .flexair-gallery-{{ section.id }} {
      position: static;
    }
    .flexair-info-title-{{ section.id }} {
      font-size: 32px;
    }
    .flexair-info-price-{{ section.id }} {
      font-size: 26px;
    }
  }
  @media screen and (max-width: 768px) {
    .flexair-product-{{ section.id }} {
      grid-template-columns: 1fr;
      gap: 32px;
    }
    .flexair-gallery-main-{{ section.id }} {
      aspect-ratio: 4 / 5;
    }
    .flexair-gallery-thumb-{{ section.id }} {
      flex: 0 0 60px;
      width: 60px;
      height: 60px;
    }
    .flexair-info-title-{{ section.id }} {
      font-size: 28px;
    }
    .flexair-info-price-{{ section.id }} {
      font-size: 24px;
    }
    .flexair-info-badges-{{ section.id }} {
      flex-direction: column;
      gap: 8px;
    }
  }
  @media screen and (max-width: 480px) {
    .section-{{ section.id }}-padding {
      padding-left: 16px;
      padding-right: 16px;
    }
    .flexair-info-title-{{ section.id }} {
      font-size: 24px;
    }
    .flexair-info-price-{{ section.id }} {
      font-size: 22px;
    }
  }

  .flexair-pay-icons-{{ section.id }} {
    display: flex;
    flex-wrap: wrap;
    align-items: center;
    gap: 6px;
    margin-top: 16px;
  }
  .flexair-pay-icons-{{ section.id }} svg.flexair-pay-icon {
    width: 38px;
    height: auto;
    border-radius: 3px;
  }
  /* Variant color swatches */
  .flexair-variants-{{ section.id }} { margin: 0 0 22px 0; }
  .flexair-variant-label-{{ section.id }} { font-size: 14px; color: #8A857D; margin-bottom: 12px; letter-spacing: 0.02em; }
  .flexair-variant-label-value-{{ section.id }} { color: #1A1816; font-weight: 600; }
  .flexair-swatches-{{ section.id }} { display: flex; flex-wrap: wrap; gap: 12px; }
  .flexair-swatch-{{ section.id }} { width: 40px; height: 40px; border-radius: 50%; border: 1px solid #E5E0D8; background: transparent; padding: 0; cursor: pointer; position: relative; display: inline-flex; align-items: center; justify-content: center; transition: border-color 0.2s ease, box-shadow 0.2s ease; }
  .flexair-swatch-{{ section.id }}:hover { border-color: #1A1816; }
  .flexair-swatch-{{ section.id }}.is-selected { border-color: #1A1816; box-shadow: 0 0 0 1px #1A1816; }
  .flexair-swatch-dot-{{ section.id }} { width: 30px; height: 30px; border-radius: 50%; display: block; }
  .flexair-swatch-{{ section.id }}.is-unavailable { cursor: not-allowed; }
  .flexair-swatch-{{ section.id }}.is-unavailable .flexair-swatch-dot-{{ section.id }} { opacity: 0.35; }
  .flexair-swatch-{{ section.id }}.is-unavailable::after { content: ''; position: absolute; top: 50%; left: 50%; width: 34px; height: 1.5px; background: #8A857D; transform: translate(-50%, -50%) rotate(-45deg); }
  {%- endstyle -%}

<div class="section-{{ section.id }}-padding">
  <!-- Breadcrumb Navigation -->
  <nav class="flexair-breadcrumb-{{ section.id }}" aria-label="Breadcrumb">
    <a href="/">Home</a>
    <span>/</span>
    <a href="/collections/all">Products</a>
    <span>/</span>
    <a href="/collections/reformers">Reformers</a>
    <span>/</span>
    <span style="color:#1A1816">{{ p.title | default: 'Sensol FLEX AIR' }}</span>
  </nav>

  <div class="flexair-product-{{ section.id }}">
    <div class="flexair-gallery-{{ section.id }}">
      <div class="flexair-gallery-main-wrapper-{{ section.id }}">
        <div class="flexair-gallery-main-{{ section.id }}" id="flexairMainImage-{{ section.id }}">
          {%- if p.featured_image -%}
            {{ p.featured_image | image_url: width: 1200 | image_tag: loading: 'eager', id: 'flexairMainImg', class: 'flexair-main-img', widths: '200,300,400,500,600,800,1000,1200', sizes: '(max-width: 768px) 100vw, 50vw' }}
          {%- else -%}
            <div style="display:flex;align-items:center;justify-content:center;height:100%;color:#8A8279">No image</div>
          {%- endif -%}
        </div>
        <button class="flexair-gallery-nav-{{ section.id }} flexair-gallery-nav-prev-{{ section.id }}" id="flexairPrev-{{ section.id }}" aria-label="Previous image">&#8249;</button>
        <button class="flexair-gallery-nav-{{ section.id }} flexair-gallery-nav-next-{{ section.id }}" id="flexairNext-{{ section.id }}" aria-label="Next image">&#8250;</button>
      </div>
      {%- if p.images.size > 1 -%}
        <div class="flexair-gallery-thumbs-{{ section.id }}" id="flexairThumbs-{{ section.id }}">
          {%- for img in p.images -%}
            <div class="flexair-gallery-thumb-{{ section.id }}{% if forloop.first %} active{% endif %}" data-image="{{ img | image_url: width: 1200 }}" data-index="{{ forloop.index0 }}">
              {{ img | image_url: width: 300 | image_tag: loading: 'lazy', class: 'flexair-thumb-img' }}
            </div>
          {%- endfor -%}
        </div>
      {%- endif -%}
    </div>

    <div class="flexair-info-{{ section.id }}">
      <div class="flexair-info-brand-{{ section.id }}">SENSOL FITNESS</div>
      <h1 class="flexair-info-title-{{ section.id }}">{{ p.title | default: 'Sensol FLEX AIR' }}</h1>
      <div class="flexair-info-subtitle-{{ section.id }}">{{ section.settings.subtitle | default: 'Your Pilates Journey Starts Here' }}</div>
      
      <!-- Judge.me Review Stars -->
      <div class="flexair-info-reviews-{{ section.id }}">
        <span class="flexair-info-stars-{{ section.id }}">★★★★★</span>
        <span>4.8 (89 reviews)</span>
      </div>
      
      <div class="flexair-info-model-{{ section.id }}">{{ section.settings.model_info | default: 'Model FLEX AIR / Smart Folding Reformer · SKU: SNSL-FLEX-AIR-AU' }}</div>

      <div class="flexair-info-price-{{ section.id }}">
        {%- if p.compare_at_price > p.price -%}
          <s>{{ p.compare_at_price | money }}</s>
        {%- endif -%}
        {{ p.price | money }} AUD
      </div>
      <div class="flexair-info-afterpay-{{ section.id }}">or 4 interest-free payments of {{ p.price | divided_by: 4 | money }} with Afterpay</div>

      <div class="flexair-info-badges-{{ section.id }}">
        <span>LIFETIME WARRANTY</span>
        <span>FREE DOMESTIC SHIPPING</span>
        <span>30-DAY MONEY-BACK</span>
      </div>

      {%- assign sv = p.selected_or_first_available_variant -%}
      {%- assign sv_sellable = false -%}
      {%- if sv.available or sv.inventory_policy == 'continue' -%}
        {%- assign sv_sellable = true -%}
      {%- endif -%}
      {%- if sv.available == false -%}
        <div class="flexair-info-stock-{{ section.id }} flexair-info-stock-out-{{ section.id }}">Sold Out</div>
      {%- elsif sv.inventory_quantity == 0 -%}
        <div class="flexair-info-stock-{{ section.id }} flexair-info-stock-in-{{ section.id }}">Available to Order</div>
      {%- else -%}
        <div class="flexair-info-stock-{{ section.id }} flexair-info-stock-in-{{ section.id }}">In Stock</div>
      {%- endif -%}

      {%- if p.options.size == 1 and p.variants.size > 1 -%}
      <div class="flexair-variants-{{ section.id }}" id="flexair-variant-picker-{{ section.id }}">
        <div class="flexair-variant-label-{{ section.id }}">
          <span class="flexair-variant-label-name-{{ section.id }}">{{ p.options_with_values.first.name }}:</span>
          <span class="flexair-variant-label-value-{{ section.id }}" data-variant-selected-name>{{ p.selected_or_first_available_variant.title }}</span>
        </div>
        <div class="flexair-swatches-{{ section.id }}" aria-label="{{ p.options_with_values.first.name | escape }}">
          {%- for v in p.variants -%}
            {%- assign c_name = v.title | strip | downcase -%}
            {%- if c_name == 'black' -%}
              {%- assign c_hex = '#1A1816' -%}
            {%- elsif c_name == 'black brown' -%}
              {%- assign c_hex = '#3D2C22' -%}
            {%- elsif c_name == 'deep brown' -%}
              {%- assign c_hex = '#5B4636' -%}
            {%- elsif c_name == 'beige' -%}
              {%- assign c_hex = '#E2D8C6' -%}
            {%- elsif c_name == 'light gray' or c_name == 'light grey' -%}
              {%- assign c_hex = '#CEC9C2' -%}
            {%- elsif c_name == 'gray' or c_name == 'grey' -%}
              {%- assign c_hex = '#A5A19A' -%}
            {%- elsif c_name == 'light green' -%}
              {%- assign c_hex = '#BCCBB2' -%}
            {%- else -%}
              {%- assign c_hex = '#A5A19A' -%}
            {%- endif -%}
            {%- if v.available or v.inventory_policy == 'continue' -%}
              {%- assign v_sellable = true -%}
            {%- else -%}
              {%- assign v_sellable = false -%}
            {%- endif -%}
            <button type="button"
              class="flexair-swatch-{{ section.id }}{% if v.id == p.selected_or_first_available_variant.id %} is-selected{% endif %}{% unless v_sellable %} is-unavailable{% endunless %}"
              data-variant-id="{{ v.id }}"
              data-variant-title="{{ v.title | escape }}"
              data-variant-price="{{ v.price | money }}"
              data-variant-compare="{% if v.compare_at_price > v.price %}{{ v.compare_at_price | money }}{% endif %}"
              data-variant-afterpay="{{ v.price | divided_by: 4 | money }}"
              data-variant-available="{% if v_sellable %}true{% else %}false{% endif %}"
              data-variant-state="{% if v.available == false %}sold_out{% elsif v.inventory_quantity == 0 %}backorder{% else %}in_stock{% endif %}"
              title="{{ v.title | escape }}"
              aria-label="{{ v.title | escape }}{% unless v_sellable %} — sold out{% endunless %}"
              aria-pressed="{% if v.id == p.selected_or_first_available_variant.id %}true{% else %}false{% endif %}">
              <span class="flexair-swatch-dot-{{ section.id }}" style="background-color: {{ c_hex }};"></span>
            </button>
          {%- endfor -%}
        </div>
      </div>
      {%- endif -%}
      {%- form 'product', p, id: 'product-form-' -%}
        <input type="hidden" name="id" value="{{ p.selected_or_first_available_variant.id }}" class="product-variant-id">
        <div class="subscriptions_app_embed_block"></div>
        <div class="flexair-qty-{{ section.id }}">
          <label class="flexair-qty-label-{{ section.id }}" for="Quantity-{{ section.id }}">Quantity</label>
          <input class="flexair-qty-input-{{ section.id }}" type="number" id="Quantity-{{ section.id }}" name="quantity" value="1" min="1">
        </div>

        <button class="flexair-add-{{ section.id }}" type="submit" name="add" {%- unless sv_sellable -%}disabled{%- endunless -%}>
          {%- if sv_sellable -%}Add to Cart{%- else -%}Sold Out{%- endif -%}
        </button>
        
        <!-- Dynamic Payment Buttons -->
        <div class="flexair-payment-buttons-{{ section.id }}">
          {{ form | payment_button }}
        </div>

        {%- if shop.enabled_payment_types.size > 0 -%}
          <div class="flexair-pay-icons-{{ section.id }}" aria-label="Supported payment methods">
            {%- for type in shop.enabled_payment_types -%}
              {{ type | payment_type_svg_tag: 'flexair-pay-icon' }}
            {%- endfor -%}
          </div>
        {%- endif -%}
      {%- endform -%}

      <ul class="flexair-features-{{ section.id }}">
        <li>Foldable frame · 165 cm folded · 0.2 m² upright storage</li>
        <li>Spring resistance (Chinese steel wire) · 7.5KG × 2 / 15KG × 2</li>
        <li>120 kg max load · Folds flat for easy storage</li>
        <li>Free Vesta Sensol App — 300+ classes, no subscription</li>
        <li>Dual-function: Pilates + strength training</li>
      </ul>
    </div>
  </div>
</div>

<!-- Lightbox -->
<div class="flexair-lightbox-{{ section.id }}" id="flexairLightbox-{{ section.id }}">
  <button class="flexair-lightbox-close-{{ section.id }}" id="flexairLightboxClose-{{ section.id }}">&times;</button>
  <button class="flexair-lightbox-nav-{{ section.id }} flexair-lightbox-nav-prev-{{ section.id }}" id="flexairLightboxPrev-{{ section.id }}" aria-label="Previous">&#8249;</button>
  <img src="" alt="Product image" id="flexairLightboxImg-{{ section.id }}">
  <button class="flexair-lightbox-nav-{{ section.id }} flexair-lightbox-nav-next-{{ section.id }}" id="flexairLightboxNext-{{ section.id }}" aria-label="Next">&#8250;</button>
</div>

<script>
(function() {
  var sectionId = '{{ section.id }}';
  var mainImg = document.getElementById('flexairMainImg');
  var mainContainer = document.getElementById('flexairMainImage-' + sectionId);
  var thumbs = document.querySelectorAll('.flexair-gallery-thumb-' + sectionId);
  var prevBtn = document.getElementById('flexairPrev-' + sectionId);
  var nextBtn = document.getElementById('flexairNext-' + sectionId);
  var lightbox = document.getElementById('flexairLightbox-' + sectionId);
  var lightboxImg = document.getElementById('flexairLightboxImg-' + sectionId);
  var lightboxClose = document.getElementById('flexairLightboxClose-' + sectionId);
  var lightboxPrev = document.getElementById('flexairLightboxPrev-' + sectionId);
  var lightboxNext = document.getElementById('flexairLightboxNext-' + sectionId);

  var images = [];
  thumbs.forEach(function(thumb) {
    images.push(thumb.getAttribute('data-image'));
  });
  if (images.length === 0 && mainImg) {
    images.push(mainImg.src);
  }
  var currentIndex = 0;

  function showImage(index) {
    if (images.length === 0) return;
    if (index < 0) index = images.length - 1;
    if (index >= images.length) index = 0;
    currentIndex = index;
    if (mainImg) {
      mainImg.src = images[index];
      mainImg.removeAttribute('srcset');
      mainImg.removeAttribute('sizes');
    }
    thumbs.forEach(function(t, i) { t.classList.toggle('active', i === index); });
    // Auto-scroll thumbs to keep active one visible
    if (thumbs[currentIndex] && thumbs[currentIndex].scrollIntoView) {
      thumbs[currentIndex].scrollIntoView({ behavior: 'smooth', block: 'nearest', inline: 'nearest' });
    }
  }

  thumbs.forEach(function(thumb, i) {
    thumb.addEventListener('click', function() { showImage(i); });
  });
  if (prevBtn) {
    prevBtn.addEventListener('click', function(e) { e.stopPropagation(); showImage(currentIndex - 1); });
  }
  if (nextBtn) {
    nextBtn.addEventListener('click', function(e) { e.stopPropagation(); showImage(currentIndex + 1); });
  }

  if (mainContainer) {
    mainContainer.addEventListener('click', function() {
      if (lightboxImg && images[currentIndex]) { lightboxImg.src = images[currentIndex]; }
      lightbox.classList.add('active');
      document.body.style.overflow = 'hidden';
    });
  }

  function closeLightbox() {
    lightbox.classList.remove('active');
    document.body.style.overflow = '';
  }
  if (lightboxClose) { lightboxClose.addEventListener('click', closeLightbox); }
  if (lightbox) {
    lightbox.addEventListener('click', function(e) { if (e.target === lightbox) closeLightbox(); });
  }
  if (lightboxPrev) {
    lightboxPrev.addEventListener('click', function(e) {
      e.stopPropagation();
      var ni = (currentIndex - 1 + images.length) % images.length;
      currentIndex = ni; lightboxImg.src = images[ni];
    });
  }
  if (lightboxNext) {
    lightboxNext.addEventListener('click', function(e) {
      e.stopPropagation();
      var ni = (currentIndex + 1) % images.length;
      currentIndex = ni; lightboxImg.src = images[ni];
    });
  }
  document.addEventListener('keydown', function(e) {
    if (!lightbox.classList.contains('active')) return;
    if (e.key === 'Escape') closeLightbox();
    if (e.key === 'ArrowLeft') {
      var ni = (currentIndex - 1 + images.length) % images.length;
      currentIndex = ni; lightboxImg.src = images[ni];
    }
    if (e.key === 'ArrowRight') {
      var ni = (currentIndex + 1) % images.length;
      currentIndex = ni; lightboxImg.src = images[ni];
    }
  });
})();
  /* Variant color swatch picker */
  (function() {
    var wrap = document.getElementById('flexair-variant-picker-{{ section.id }}');
    if (!wrap) return;
    var form = wrap.parentNode.querySelector('form');
    if (!form) return;
    var idInput = form.querySelector('.product-variant-id');
    var addBtn = form.querySelector('button[name="add"]');
    var labelValue = wrap.querySelector('[data-variant-selected-name]');
    var root = wrap.parentNode;
    var priceEl = root.querySelector('.flexair-info-price-{{ section.id }}');
    var afterpayEl = root.querySelector('.flexair-info-afterpay-{{ section.id }}');
    var stockEl = root.querySelector('.flexair-info-stock-{{ section.id }}');
    var swatches = wrap.querySelectorAll('.flexair-swatch-{{ section.id }}');
    swatches.forEach(function(btn) {
      btn.addEventListener('click', function() {
        swatches.forEach(function(b) {
          b.classList.remove('is-selected');
          b.setAttribute('aria-pressed', 'false');
        });
        btn.classList.add('is-selected');
        btn.setAttribute('aria-pressed', 'true');
        if (idInput) idInput.value = btn.getAttribute('data-variant-id');
        if (labelValue) labelValue.textContent = btn.getAttribute('data-variant-title');
        var avail = btn.getAttribute('data-variant-available') === 'true';
        var state = btn.getAttribute('data-variant-state') || (avail ? 'in_stock' : 'sold_out');
        if (priceEl) {
          var compare = btn.getAttribute('data-variant-compare');
          var price = btn.getAttribute('data-variant-price');
          priceEl.innerHTML = (compare ? '<s>' + compare + '</s> ' : '') + price + ' AUD';
        }
        if (afterpayEl) afterpayEl.textContent = 'or 4 interest-free payments of ' + btn.getAttribute('data-variant-afterpay') + ' with Afterpay';
        if (stockEl) {
          stockEl.classList.toggle('flexair-info-stock-in-{{ section.id }}', avail);
          stockEl.classList.toggle('flexair-info-stock-out-{{ section.id }}', !avail);
          stockEl.textContent = state === 'in_stock' ? 'In Stock' : (state === 'backorder' ? 'Available to Order' : 'Sold Out');
        }
        if (addBtn) {
          addBtn.disabled = !avail;
          addBtn.textContent = avail ? 'Add to Cart' : 'Sold Out';
        }
      });
    });
  })();
</script>

{% schema %}
{
  "name": "Sensol FLEX AIR Landing",
  "tag": "section",
  "class": "flexair-landing-section",
  "settings": [
    {
      "type": "header",
      "content": "Product Landing Section"
    },
    {
      "type": "text",
      "id": "subtitle",
      "label": "Subtitle",
      "default": "Your Pilates Journey Starts Here"
    },
    {
      "type": "text",
      "id": "model_info",
      "label": "Model Info",
      "default": "Model FLEX AIR / Smart Folding Reformer · SKU: SNSL-FLEX-AIR-AU"
    },
    {
      "type": "range",
      "id": "padding_top",
      "min": 0,
      "max": 200,
      "step": 10,
      "unit": "px",
      "label": "Top padding",
      "default": 140
    },
    {
      "type": "range",
      "id": "padding_bottom",
      "min": 0,
      "max": 200,
      "step": 10,
      "unit": "px",
      "label": "Bottom padding",
      "default": 80
    }
  ],
  "presets": [
    {
      "name": "Sensol FLEX AIR Landing"
    }
  ]
}
{% endschema %}
