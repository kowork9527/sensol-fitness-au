{%- liquid
  assign p = product
-%}
{%- style -%}
  .section-{{ section.id }}-padding {
    padding-top: {{ section.settings.padding_top }}px;
    padding-bottom: {{ section.settings.padding_bottom | default: 80 }}px;
    padding-left: 20px;
    padding-right: 20px;
    background: #F7F4F0;
  }
  .flexpro-breadcrumb-{{ section.id }} {
    max-width: 1200px;
    margin: 0 auto 24px;
    font-size: 12px;
    color: #8A8279;
    letter-spacing: 0.05em;
  }
  .flexpro-breadcrumb-{{ section.id }} a {
    color: #8A8279;
    text-decoration: none;
    transition: color 0.2s;
  }
  .flexpro-breadcrumb-{{ section.id }} a:hover {
    color: #1A1816;
  }
  .flexpro-breadcrumb-{{ section.id }} span {
    margin: 0 8px;
  }
  .flexpro-product-{{ section.id }} {
    max-width: 1200px;
    margin: 0 auto;
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 60px;
    align-items: start;
    font-family: 'Inter', -apple-system, BlinkMacSystemFont, sans-serif;
    color: #1A1816;
  }
  .flexpro-gallery-{{ section.id }} {
    position: sticky;
    top: 20px;
  }
  .flexpro-gallery-main-{{ section.id }} {
    aspect-ratio: 1 / 1;
    background: #fff;
    overflow: hidden;
    margin-bottom: 16px;
    cursor: zoom-in;
    position: relative;
  }
  .flexpro-gallery-main-{{ section.id }} img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    transition: transform 0.3s;
  }
  .flexpro-gallery-main-{{ section.id }}:hover img {
    transform: scale(1.02);
  }
  .flexpro-gallery-thumbs-{{ section.id }} {
    display: flex;
    gap: 10px;
    overflow-x: auto;
    scroll-behavior: smooth;
    -webkit-overflow-scrolling: touch;
    padding-bottom: 6px;
  }
  .flexpro-gallery-thumbs-{{ section.id }}::-webkit-scrollbar {
    height: 4px;
  }
  .flexpro-gallery-thumbs-{{ section.id }}::-webkit-scrollbar-track {
    background: transparent;
  }
  .flexpro-gallery-thumbs-{{ section.id }}::-webkit-scrollbar-thumb {
    background: rgba(0,0,0,0.15);
    border-radius: 2px;
  }
  .flexpro-gallery-thumb-{{ section.id }} {
    flex: 0 0 72px;
    width: 72px;
    height: 72px;
    background: #fff;
    overflow: hidden;
    cursor: pointer;
    border: 2px solid transparent;
    border-radius: 6px;
    transition: border-color 0.2s;
  }
  .flexpro-gallery-thumb-{{ section.id }}:hover,
  .flexpro-gallery-thumb-{{ section.id }}.active {
    border-color: #1A1816;
  }
  .flexpro-gallery-thumb-{{ section.id }} img {
    width: 100%;
    height: 100%;
    object-fit: cover;
  }
  .flexpro-gallery-main-wrapper-{{ section.id }} {
    position: relative;
    margin-bottom: 16px;
  }
  .flexpro-gallery-nav-{{ section.id }} {
    position: absolute;
    top: 50%;
    transform: translateY(-50%);
    width: 48px;
    height: 48px;
    background: rgba(255,255,255,0.92);
    border: none;
    border-radius: 50%;
    font-size: 28px;
    line-height: 1;
    cursor: pointer;
    display: flex;
    align-items: center;
    justify-content: center;
    z-index: 10;
    opacity: 0.65;
    transition: opacity 0.3s, background 0.2s;
    color: #1A1816;
  }
  .flexpro-gallery-main-wrapper-{{ section.id }}:hover .flexpro-gallery-nav-{{ section.id }} {
    opacity: 1;
  }
  .flexpro-gallery-nav-{{ section.id }}:hover {
    background: rgba(255,255,255,1);
  }
  .flexpro-gallery-nav-prev-{{ section.id }} { left: 12px; }
  .flexpro-gallery-nav-next-{{ section.id }} { right: 12px; }

  .flexpro-lightbox-nav-{{ section.id }} {
    position: absolute;
    top: 50%;
    transform: translateY(-50%);
    width: 56px;
    height: 56px;
    background: rgba(255,255,255,0.15);
    border: 1px solid rgba(255,255,255,0.3);
    border-radius: 50%;
    color: #fff;
    font-size: 32px;
    cursor: pointer;
    display: flex;
    align-items: center;
    justify-content: center;
    z-index: 10001;
    transition: background 0.2s;
  }
  .flexpro-lightbox-nav-{{ section.id }}:hover {
    background: rgba(255,255,255,0.3);
  }
  .flexpro-lightbox-nav-prev-{{ section.id }} { left: 30px; }
  .flexpro-lightbox-nav-next-{{ section.id }} { right: 30px; }
  .flexpro-info-{{ section.id }} {
    padding: 20px 0;
  }
  .flexpro-info-brand-{{ section.id }} {
    font-size: 11px;
    font-weight: 500;
    letter-spacing: 0.2em;
    text-transform: uppercase;
    color: #8A8279;
    margin-bottom: 12px;
  }
  .flexpro-info-title-{{ section.id }} {
    font-size: 36px;
    font-weight: 300;
    letter-spacing: 0.04em;
    margin: 0 0 8px;
    color: #1A1816;
  }
  .flexpro-info-subtitle-{{ section.id }} {
    font-size: 16px;
    color: #8A8279;
    margin-bottom: 16px;
  }
  .flexpro-info-reviews-{{ section.id }} {
    display: flex;
    align-items: center;
    gap: 8px;
    margin-bottom: 16px;
    font-size: 13px;
    color: #8A8279;
  }
  .flexpro-info-stars-{{ section.id }} {
    color: #F5A623;
    letter-spacing: 2px;
  }
  .flexpro-info-model-{{ section.id }} {
    font-size: 12px;
    color: #8A8279;
    letter-spacing: 0.05em;
    margin-bottom: 24px;
    padding-bottom: 24px;
    border-bottom: 1px solid #D4CEC6;
  }
  .flexpro-info-price-{{ section.id }} {
    font-size: 28px;
    font-weight: 400;
    color: #1A1816;
    margin-bottom: 8px;
    letter-spacing: 0.02em;
  }
  .flexpro-info-price-{{ section.id }} s {
    color: #8A8279;
    margin-right: 12px;
    font-size: 18px;
  }
  .flexpro-info-afterpay-{{ section.id }} {
    font-size: 13px;
    color: #8A8279;
    margin-bottom: 24px;
  }
  .flexpro-info-badges-{{ section.id }} {
    display: flex;
    gap: 16px;
    flex-wrap: wrap;
    font-size: 11px;
    letter-spacing: 0.1em;
    text-transform: uppercase;
    color: #8A8279;
    margin-bottom: 24px;
    padding-bottom: 24px;
    border-bottom: 1px solid #D4CEC6;
  }
  .flexpro-info-stock-{{ section.id }} {
    font-size: 12px;
    letter-spacing: 0.15em;
    text-transform: uppercase;
    margin-bottom: 24px;
    padding: 8px 16px;
    display: inline-block;
  }
  .flexpro-info-stock-in-{{ section.id }} {
    background: #E8F5E9;
    color: #2E7D32;
  }
  .flexpro-info-stock-out-{{ section.id }} {
    background: #FFEBEE;
    color: #C62828;
  }
  .flexpro-qty-{{ section.id }} {
    display: flex;
    align-items: center;
    gap: 16px;
    margin-bottom: 16px;
  }
  .flexpro-qty-label-{{ section.id }} {
    font-size: 12px;
    letter-spacing: 0.15em;
    text-transform: uppercase;
    color: #8A8279;
  }
  .flexpro-qty-input-{{ section.id }} {
    width: 80px;
    padding: 12px;
    border: 1px solid #D4CEC6;
    background: #fff;
    font-size: 16px;
    text-align: center;
  }
  .flexpro-add-{{ section.id }} {
    width: 100%;
    padding: 20px;
    background: #1A1816;
    color: #F7F4F0;
    border: none;
    font-size: 14px;
    letter-spacing: 0.2em;
    text-transform: uppercase;
    cursor: pointer;
    transition: background 0.3s;
    font-weight: 500;
    margin-bottom: 12px;
  }
  .flexpro-add-{{ section.id }}:hover {
    background: #2A2826;
  }
  .flexpro-add-{{ section.id }}:disabled {
    background: #8A8279;
    cursor: not-allowed;
  }
  .flexpro-payment-buttons-{{ section.id }} {
    width: 100%;
    margin-bottom: 32px;
  }
  .flexpro-payment-buttons-{{ section.id }} .shopify-payment-button {
    width: 100%;
  }
  .flexpro-payment-buttons-{{ section.id }} .shopify-payment-button__button {
    width: 100%;
    padding: 16px;
    background: #fff;
    border: 1px solid #D4CEC6;
    font-size: 12px;
    letter-spacing: 0.1em;
    text-transform: uppercase;
    color: #8A8279;
    text-align: center;
  }
  .flexpro-features-{{ section.id }} {
    list-style: none;
    padding: 0;
    margin: 0;
  }
  .flexpro-features-{{ section.id }} li {
    font-size: 14px;
    line-height: 1.6;
    color: #1A1816;
    padding: 8px 0;
    padding-left: 20px;
    position: relative;
  }
  .flexpro-features-{{ section.id }} li::before {
    content: '\2022';
    position: absolute;
    left: 0;
    color: #8A8279;
  }
  .flexpro-lightbox-{{ section.id }} {
    display: none;
    position: fixed;
    inset: 0;
    background: rgba(0,0,0,0.95);
    z-index: 9999;
    align-items: center;
    justify-content: center;
    cursor: zoom-out;
  }
  .flexpro-lightbox-{{ section.id }}.active {
    display: flex;
  }
  .flexpro-lightbox-{{ section.id }} img {
    max-width: 90vw;
    max-height: 90vh;
    object-fit: contain;
  }
  .flexpro-lightbox-close-{{ section.id }} {
    position: absolute;
    top: 20px;
    right: 20px;
    width: 40px;
    height: 40px;
    background: none;
    border: 1px solid #fff;
    color: #fff;
    font-size: 24px;
    cursor: pointer;
    display: flex;
    align-items: center;
    justify-content: center;
  }
  @media screen and (max-width: 768px) {
    .flexpro-product-{{ section.id }} {
      grid-template-columns: 1fr;
      gap: 32px;
    }
    .flexpro-gallery-{{ section.id }} {
      position: static;
    }
    .flexpro-info-title-{{ section.id }} {
      font-size: 28px;
    }
    .flexpro-info-price-{{ section.id }} {
      font-size: 24px;
    }
    .flexpro-info-badges-{{ section.id }} {
      flex-direction: column;
      gap: 8px;
    }
  }
{%- endstyle -%}

<div class="section-{{ section.id }}-padding">
  <nav class="flexpro-breadcrumb-{{ section.id }}" aria-label="Breadcrumb">
    <a href="/">Home</a>
    <span>/</span>
    <a href="/collections/all">Products</a>
    <span>/</span>
    <a href="/collections/reformers">Reformers</a>
    <span>/</span>
    <span style="color:#1A1816">{{ p.title | default: 'Sensol FLEX PRO' }}</span>
  </nav>

  <div class="flexpro-product-{{ section.id }}">
    <div class="flexpro-gallery-{{ section.id }}">
      <div class="flexpro-gallery-main-wrapper-{{ section.id }}">
        <div class="flexpro-gallery-main-{{ section.id }}" id="flexproMainImage-{{ section.id }}">
          {%- if p.featured_image -%}
            {{ p.featured_image | image_url: width: 1200 | image_tag: loading: 'eager', id: 'flexproMainImg' }}
          {%- else -%}
            <div style="display:flex;align-items:center;justify-content:center;height:100%;color:#8A8279">No image</div>
          {%- endif -%}
        </div>
        <button class="flexpro-gallery-nav-{{ section.id }} flexpro-gallery-nav-prev-{{ section.id }}" id="flexproPrev-{{ section.id }}" aria-label="Previous image">&#8249;</button>
        <button class="flexpro-gallery-nav-{{ section.id }} flexpro-gallery-nav-next-{{ section.id }}" id="flexproNext-{{ section.id }}" aria-label="Next image">&#8250;</button>
      </div>
      {%- if p.images.size > 1 -%}
        <div class="flexpro-gallery-thumbs-{{ section.id }}" id="flexproThumbs-{{ section.id }}">
          {%- for img in p.images -%}
            <div class="flexpro-gallery-thumb-{{ section.id }}{% if forloop.first %} active{% endif %}" data-image="{{ img | image_url: width: 1200 }}" data-index="{{ forloop.index0 }}">
              {{ img | image_url: width: 300 | image_tag: loading: 'lazy' }}
            </div>
          {%- endfor -%}
        </div>
      {%- endif -%}
    </div>

    <div class="flexpro-info-{{ section.id }}">
      <div class="flexpro-info-brand-{{ section.id }}">SENSOL FITNESS</div>
      <h1 class="flexpro-info-title-{{ section.id }}">{{ p.title | default: 'Sensol FLEX PRO' }}</h1>
      <div class="flexpro-info-subtitle-{{ section.id }}">{{ section.settings.subtitle | default: 'Studio Power. Home Convenience.' }}</div>
      
      <div class="flexpro-info-reviews-{{ section.id }}">
        <span class="flexpro-info-stars-{{ section.id }}">&#9733;&#9733;&#9733;&#9733;&#9733;</span>
        <span>4.9 (64 reviews)</span>
      </div>
      
      <div class="flexpro-info-model-{{ section.id }}">{{ section.settings.model_info | default: 'Model FLEX PRO / Smart Spring Reformer · SKU: SNSL-FLEX-PRO-AU' }}</div>

      <div class="flexpro-info-price-{{ section.id }}">
        {%- if p.compare_at_price > p.price -%}
          <s>{{ p.compare_at_price | money }}</s>
        {%- endif -%}
        {{ p.price | money }} AUD
      </div>
      <div class="flexpro-info-afterpay-{{ section.id }}">or 4 interest-free payments of {{ p.price | divided_by: 4 | money }} with Afterpay</div>

      <div class="flexpro-info-badges-{{ section.id }}">
        <span>LIFETIME WARRANTY</span>
        <span>FREE DOMESTIC SHIPPING</span>
        <span>30-DAY MONEY-BACK</span>
      </div>

      {%- if p.available -%}
        <div class="flexpro-info-stock-{{ section.id }} flexpro-info-stock-in-{{ section.id }}">In Stock</div>
      {%- else -%}
        <div class="flexpro-info-stock-{{ section.id }} flexpro-info-stock-out-{{ section.id }}">Sold Out</div>
      {%- endif -%}

      {%- form 'product', p, id: 'product-form-flexpro-' -%}
        <div class="subscriptions_app_embed_block"></div>
        <div class="flexpro-qty-{{ section.id }}">
          <label class="flexpro-qty-label-{{ section.id }}" for="Quantity-fp-{{ section.id }}">Quantity</label>
          <input class="flexpro-qty-input-{{ section.id }}" type="number" id="Quantity-fp-{{ section.id }}" name="quantity" value="1" min="1">
        </div>

        <button class="flexpro-add-{{ section.id }}" type="submit" name="add" {%- unless p.available -%}disabled{%- endunless -%}>
          {%- if p.available -%}Add to Cart{%- else -%}Sold Out{%- endif -%}
        </button>
        
        <div class="flexpro-payment-buttons-{{ section.id }}">
          {{ form | payment_button }}
        </div>
      {%- endform -%}

      <ul class="flexpro-features-{{ section.id }}">
        <li>150 kg max load — German steel wire springs · 7.5KG × 2 / 15KG × 2</li>
        <li>Dual rear columns for hand and foot rope functions</li>
        <li>Sliding bed: 460 × 900mm · Max stroke: 860mm</li>
        <li>Dual-function: Reformer + Strength · 200+ movements</li>
        <li>Free Vesta Sensol App — 300+ classes, no subscription</li>
      </ul>
    </div>
  </div>
</div>

<!-- Lightbox -->
<div class="flexpro-lightbox-{{ section.id }}" id="flexproLightbox-{{ section.id }}">
  <button class="flexpro-lightbox-close-{{ section.id }}" id="flexproLightboxClose-{{ section.id }}">&times;</button>
  <button class="flexpro-lightbox-nav-{{ section.id }} flexpro-lightbox-nav-prev-{{ section.id }}" id="flexproLightboxPrev-{{ section.id }}" aria-label="Previous">&#8249;</button>
  <img src="" alt="Product image" id="flexproLightboxImg-{{ section.id }}">
  <button class="flexpro-lightbox-nav-{{ section.id }} flexpro-lightbox-nav-next-{{ section.id }}" id="flexproLightboxNext-{{ section.id }}" aria-label="Next">&#8250;</button>
</div>

<script>
(function() {
  var sectionId = '{{ section.id }}';
  var mainImg = document.getElementById('flexproMainImg');
  var mainContainer = document.getElementById('flexproMainImage-' + sectionId);
  var thumbs = document.querySelectorAll('.flexpro-gallery-thumb-' + sectionId);
  var prevBtn = document.getElementById('flexproPrev-' + sectionId);
  var nextBtn = document.getElementById('flexproNext-' + sectionId);
  var lightbox = document.getElementById('flexproLightbox-' + sectionId);
  var lightboxImg = document.getElementById('flexproLightboxImg-' + sectionId);
  var lightboxClose = document.getElementById('flexproLightboxClose-' + sectionId);
  var lightboxPrev = document.getElementById('flexproLightboxPrev-' + sectionId);
  var lightboxNext = document.getElementById('flexproLightboxNext-' + sectionId);

  var images = [];
  thumbs.forEach(function(thumb) {
    images.push(thumb.getAttribute('data-image'));
  });
  if (images.length === 0 && mainImg) {
    images.push(mainImg.src);
  }
  var currentIndex = 0;

  function showImage(index) {
    if (images.length === 0) return;
    if (index < 0) index = images.length - 1;
    if (index >= images.length) index = 0;
    currentIndex = index;
    if (mainImg) {
      mainImg.src = images[index];
      mainImg.removeAttribute('srcset');
      mainImg.removeAttribute('sizes');
    }
    thumbs.forEach(function(t, i) { t.classList.toggle('active', i === index); });
    // Auto-scroll thumbs to keep active one visible
    if (thumbs[currentIndex] && thumbs[currentIndex].scrollIntoView) {
      thumbs[currentIndex].scrollIntoView({ behavior: 'smooth', block: 'nearest', inline: 'nearest' });
    }
  }

  thumbs.forEach(function(thumb, i) {
    thumb.addEventListener('click', function() { showImage(i); });
  });
  if (prevBtn) {
    prevBtn.addEventListener('click', function(e) { e.stopPropagation(); showImage(currentIndex - 1); });
  }
  if (nextBtn) {
    nextBtn.addEventListener('click', function(e) { e.stopPropagation(); showImage(currentIndex + 1); });
  }

  if (mainContainer) {
    mainContainer.addEventListener('click', function() {
      if (lightboxImg && images[currentIndex]) { lightboxImg.src = images[currentIndex]; }
      lightbox.classList.add('active');
      document.body.style.overflow = 'hidden';
    });
  }

  function closeLightbox() {
    lightbox.classList.remove('active');
    document.body.style.overflow = '';
  }
  if (lightboxClose) { lightboxClose.addEventListener('click', closeLightbox); }
  if (lightbox) {
    lightbox.addEventListener('click', function(e) { if (e.target === lightbox) closeLightbox(); });
  }
  if (lightboxPrev) {
    lightboxPrev.addEventListener('click', function(e) {
      e.stopPropagation();
      var ni = (currentIndex - 1 + images.length) % images.length;
      currentIndex = ni; lightboxImg.src = images[ni];
    });
  }
  if (lightboxNext) {
    lightboxNext.addEventListener('click', function(e) {
      e.stopPropagation();
      var ni = (currentIndex + 1) % images.length;
      currentIndex = ni; lightboxImg.src = images[ni];
    });
  }
  document.addEventListener('keydown', function(e) {
    if (!lightbox.classList.contains('active')) return;
    if (e.key === 'Escape') closeLightbox();
    if (e.key === 'ArrowLeft') {
      var ni = (currentIndex - 1 + images.length) % images.length;
      currentIndex = ni; lightboxImg.src = images[ni];
    }
    if (e.key === 'ArrowRight') {
      var ni = (currentIndex + 1) % images.length;
      currentIndex = ni; lightboxImg.src = images[ni];
    }
  });
})();
</script>

{% schema %}
{
  "name": "Sensol FLEX PRO Landing",
  "tag": "section",
  "class": "flex-pro-landing-section",
  "settings": [
    {
      "type": "header",
      "content": "Product Landing Section"
    },
    {
      "type": "text",
      "id": "subtitle",
      "label": "Subtitle",
      "default": "Studio Power. Home Convenience."
    },
    {
      "type": "text",
      "id": "model_info",
      "label": "Model Info",
      "default": "Model FLEX PRO / Smart Spring Reformer · SKU: SNSL-FLEX-PRO-AU"
    },
    {
      "type": "range",
      "id": "padding_top",
      "min": 0,
      "max": 200,
      "step": 10,
      "unit": "px",
      "label": "Top padding",
      "default": 40
    },
    {
      "type": "range",
      "id": "padding_bottom",
      "min": 0,
      "max": 200,
      "step": 10,
      "unit": "px",
      "label": "Bottom padding",
      "default": 80
    }
  ],
  "presets": [
    {
      "name": "Sensol FLEX PRO Landing"
    }
  ]
}
{% endschema %}
