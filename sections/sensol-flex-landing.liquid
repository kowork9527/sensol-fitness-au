{%- liquid
  assign p = product
-%}
{%- style -%}
  .section-{{ section.id }}-padding {
    padding-top: {{ section.settings.padding_top }}px;
    padding-bottom: {{ section.settings.padding_bottom | default: 80 }}px;
    padding-left: 20px;
    padding-right: 20px;
    background: #F7F4F0;
    box-sizing: border-box;
  }
  .section-{{ section.id }}-padding *,
  .section-{{ section.id }}-padding *::before,
  .section-{{ section.id }}-padding *::after {
    box-sizing: border-box;
  }
  /* Breadcrumb */
  .flex-breadcrumb-{{ section.id }} {
    max-width: 1200px;
    margin: 0 auto 24px;
    font-size: 12px;
    color: #8A8279;
    letter-spacing: 0.05em;
  }
  .flex-breadcrumb-{{ section.id }} a {
    color: #8A8279;
    text-decoration: none;
    transition: color 0.2s;
  }
  .flex-breadcrumb-{{ section.id }} a:hover {
    color: #1A1816;
  }
  .flex-breadcrumb-{{ section.id }} span {
    margin: 0 8px;
  }
  .flex-product-{{ section.id }} {
    max-width: 1200px;
    margin: 0 auto;
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 60px;
    align-items: start;
    font-family: 'Inter', -apple-system, BlinkMacSystemFont, sans-serif;
    color: #1A1816;
    min-width: 0;
  }
  .flex-gallery-{{ section.id }} {
    position: sticky;
    top: 20px;
    min-width: 0;
    max-width: 100%;
  }
  .flex-gallery-main-{{ section.id }} {
    aspect-ratio: 1 / 1;
    background: #fff;
    overflow: hidden;
    margin-bottom: 16px;
    cursor: zoom-in;
    position: relative;
    min-width: 0;
    max-width: 100%;
  }
  .flex-gallery-main-{{ section.id }} img {
    width: 100% !important;
    height: 100% !important;
    max-width: 100% !important;
    object-fit: cover;
    display: block;
    transition: transform 0.3s;
  }
  .flex-gallery-main-{{ section.id }}:hover img {
    transform: scale(1.02);
  }
  .flex-gallery-thumbs-{{ section.id }} {
    display: flex;
    gap: 10px;
    overflow-x: auto;
    scroll-behavior: smooth;
    -webkit-overflow-scrolling: touch;
    padding-bottom: 6px;
  }
  .flex-gallery-thumbs-{{ section.id }}::-webkit-scrollbar {
    height: 4px;
  }
  .flex-gallery-thumbs-{{ section.id }}::-webkit-scrollbar-track {
    background: transparent;
  }
  .flex-gallery-thumbs-{{ section.id }}::-webkit-scrollbar-thumb {
    background: rgba(0,0,0,0.15);
    border-radius: 2px;
  }
  .flex-gallery-thumb-{{ section.id }} {
    flex: 0 0 72px;
    width: 72px;
    height: 72px;
    background: #fff;
    overflow: hidden;
    cursor: pointer;
    border: 2px solid transparent;
    border-radius: 6px;
    transition: border-color 0.2s;
  }
  .flex-gallery-thumb-{{ section.id }}:hover,
  .flex-gallery-thumb-{{ section.id }}.active {
    border-color: #1A1816;
  }
  .flex-gallery-thumb-{{ section.id }} img {
    width: 100% !important;
    height: 100% !important;
    max-width: 100% !important;
    object-fit: cover;
    display: block;
  }
  .flex-gallery-main-wrapper-{{ section.id }} {
    position: relative;
    margin-bottom: 16px;
    min-width: 0;
    max-width: 100%;
  }
  .flex-gallery-nav-{{ section.id }} {
    position: absolute;
    top: 50%;
    transform: translateY(-50%);
    width: 48px;
    height: 48px;
    background: rgba(255,255,255,0.92);
    border: none;
    border-radius: 50%;
    font-size: 28px;
    line-height: 1;
    cursor: pointer;
    display: flex;
    align-items: center;
    justify-content: center;
    z-index: 10;
    opacity: 0.65;
    transition: opacity 0.3s, background 0.2s;
    color: #1A1816;
  }
  .flex-gallery-main-wrapper-{{ section.id }}:hover .flex-gallery-nav-{{ section.id }} {
    opacity: 1;
  }
  .flex-gallery-nav-{{ section.id }}:hover {
    background: rgba(255,255,255,1);
  }
  .flex-gallery-nav-prev-{{ section.id }} { left: 12px; }
  .flex-gallery-nav-next-{{ section.id }} { right: 12px; }

  .flex-lightbox-nav-{{ section.id }} {
    position: absolute;
    top: 50%;
    transform: translateY(-50%);
    width: 56px;
    height: 56px;
    background: rgba(255,255,255,0.15);
    border: 1px solid rgba(255,255,255,0.3);
    border-radius: 50%;
    color: #fff;
    font-size: 32px;
    cursor: pointer;
    display: flex;
    align-items: center;
    justify-content: center;
    z-index: 10001;
    transition: background 0.2s;
  }
  .flex-lightbox-nav-{{ section.id }}:hover {
    background: rgba(255,255,255,0.3);
  }
  .flex-lightbox-nav-prev-{{ section.id }} { left: 30px; }
  .flex-lightbox-nav-next-{{ section.id }} { right: 30px; }
  .flex-info-{{ section.id }} {
    padding: 20px 0;
    min-width: 0;
  }
  .flex-info-brand-{{ section.id }} {
    font-size: 11px;
    font-weight: 500;
    letter-spacing: 0.2em;
    text-transform: uppercase;
    color: #8A8279;
    margin-bottom: 12px;
  }
  .flex-info-title-{{ section.id }} {
    font-size: 36px;
    font-weight: 300;
    letter-spacing: 0.04em;
    margin: 0 0 8px;
    color: #1A1816;
  }
  .flex-info-subtitle-{{ section.id }} {
    font-size: 16px;
    color: #8A8279;
    margin-bottom: 16px;
  }
  .flex-info-reviews-{{ section.id }} {
    display: flex;
    align-items: center;
    gap: 8px;
    margin-bottom: 16px;
    font-size: 13px;
    color: #8A8279;
  }
  .flex-info-stars-{{ section.id }} {
    color: #F5A623;
    letter-spacing: 2px;
  }
  .flex-info-model-{{ section.id }} {
    font-size: 12px;
    color: #8A8279;
    letter-spacing: 0.05em;
    margin-bottom: 24px;
    padding-bottom: 24px;
    border-bottom: 1px solid #D4CEC6;
  }
  .flex-info-price-{{ section.id }} {
    font-size: 28px;
    font-weight: 400;
    color: #1A1816;
    margin-bottom: 8px;
    letter-spacing: 0.02em;
  }
  .flex-info-price-{{ section.id }} s {
    color: #8A8279;
    margin-right: 12px;
    font-size: 18px;
  }
  .flex-info-afterpay-{{ section.id }} {
    font-size: 13px;
    color: #8A8279;
    margin-bottom: 24px;
  }
  .flex-info-badges-{{ section.id }} {
    display: flex;
    gap: 16px;
    flex-wrap: wrap;
    font-size: 11px;
    letter-spacing: 0.1em;
    text-transform: uppercase;
    color: #8A8279;
    margin-bottom: 24px;
    padding-bottom: 24px;
    border-bottom: 1px solid #D4CEC6;
  }
  .flex-info-stock-{{ section.id }} {
    font-size: 12px;
    letter-spacing: 0.15em;
    text-transform: uppercase;
    margin-bottom: 24px;
    padding: 8px 16px;
    display: inline-block;
  }
  .flex-info-stock-in-{{ section.id }} {
    background: #E8F5E9;
    color: #2E7D32;
  }
  .flex-info-stock-out-{{ section.id }} {
    background: #FFEBEE;
    color: #C62828;
  }
  .flex-qty-{{ section.id }} {
    display: flex;
    align-items: center;
    gap: 16px;
    margin-bottom: 16px;
  }
  .flex-qty-label-{{ section.id }} {
    font-size: 12px;
    letter-spacing: 0.15em;
    text-transform: uppercase;
    color: #8A8279;
  }
  .flex-qty-input-{{ section.id }} {
    width: 80px;
    padding: 12px;
    border: 1px solid #D4CEC6;
    background: #fff;
    font-size: 16px;
    text-align: center;
  }
  .flex-add-{{ section.id }} {
    width: 100%;
    padding: 20px;
    background: #1A1816;
    color: #F7F4F0;
    border: none;
    font-size: 14px;
    letter-spacing: 0.2em;
    text-transform: uppercase;
    cursor: pointer;
    transition: background 0.3s;
    font-weight: 500;
    margin-bottom: 12px;
  }
  .flex-add-{{ section.id }}:hover {
    background: #2A2826;
  }
  .flex-add-{{ section.id }}:disabled {
    background: #8A8279;
    cursor: not-allowed;
  }
  .flex-payment-buttons-{{ section.id }} {
    width: 100%;
    margin-bottom: 32px;
  }
  .flex-payment-buttons-{{ section.id }} .shopify-payment-button {
    width: 100%;
  }
  .flex-payment-buttons-{{ section.id }} .shopify-payment-button__button {
    width: 100%;
    padding: 16px;
    background: #fff;
    border: 1px solid #D4CEC6;
    font-size: 12px;
    letter-spacing: 0.1em;
    text-transform: uppercase;
    color: #8A8279;
    text-align: center;
  }
  .flex-features-{{ section.id }} {
    list-style: none;
    padding: 0;
    margin: 0;
  }
  .flex-features-{{ section.id }} li {
    font-size: 14px;
    line-height: 1.6;
    color: #1A1816;
    padding: 8px 0;
    padding-left: 20px;
    position: relative;
  }
  .flex-features-{{ section.id }} li::before {
    content: '•';
    position: absolute;
    left: 0;
    color: #8A8279;
  }
  /* Lightbox */
  .flex-lightbox-{{ section.id }} {
    display: none;
    position: fixed;
    inset: 0;
    background: rgba(0,0,0,0.95);
    z-index: 9999;
    align-items: center;
    justify-content: center;
    cursor: zoom-out;
  }
  .flex-lightbox-{{ section.id }}.active {
    display: flex;
  }
  .flex-lightbox-{{ section.id }} img {
    max-width: 90vw;
    max-height: 90vh;
    object-fit: contain;
  }
  .flex-lightbox-close-{{ section.id }} {
    position: absolute;
    top: 20px;
    right: 20px;
    width: 40px;
    height: 40px;
    background: none;
    border: 1px solid #fff;
    color: #fff;
    font-size: 24px;
    cursor: pointer;
    display: flex;
    align-items: center;
    justify-content: center;
  }
  @media screen and (max-width: 990px) {
    .flex-product-{{ section.id }} {
      grid-template-columns: 1fr;
      gap: 40px;
    }
    .flex-gallery-{{ section.id }} {
      position: static;
    }
    .flex-info-title-{{ section.id }} {
      font-size: 32px;
    }
    .flex-info-price-{{ section.id }} {
      font-size: 26px;
    }
  }
  @media screen and (max-width: 768px) {
    .flex-product-{{ section.id }} {
      grid-template-columns: 1fr;
      gap: 32px;
    }
    .flex-gallery-main-{{ section.id }} {
      aspect-ratio: 4 / 5;
    }
    .flex-gallery-thumb-{{ section.id }} {
      flex: 0 0 60px;
      width: 60px;
      height: 60px;
    }
    .flex-info-title-{{ section.id }} {
      font-size: 28px;
    }
    .flex-info-price-{{ section.id }} {
      font-size: 24px;
    }
    .flex-info-badges-{{ section.id }} {
      flex-direction: column;
      gap: 8px;
    }
  }
  @media screen and (max-width: 480px) {
    .section-{{ section.id }}-padding {
      padding-left: 16px;
      padding-right: 16px;
    }
    .flex-info-title-{{ section.id }} {
      font-size: 24px;
    }
    .flex-info-price-{{ section.id }} {
      font-size: 22px;
    }
  }
{%- endstyle -%}

<div class="section-{{ section.id }}-padding">
  <!-- Breadcrumb Navigation -->
  <nav class="flex-breadcrumb-{{ section.id }}" aria-label="Breadcrumb">
    <a href="/">Home</a>
    <span>/</span>
    <a href="/collections/all">Products</a>
    <span>/</span>
    <a href="/collections/reformers">Reformers</a>
    <span>/</span>
    <span style="color:#1A1816">{{ p.title | default: 'Sensol FLEX' }}</span>
  </nav>

  <div class="flex-product-{{ section.id }}">
    <div class="flex-gallery-{{ section.id }}">
      <div class="flex-gallery-main-wrapper-{{ section.id }}">
        <div class="flex-gallery-main-{{ section.id }}" id="flexMainImage-{{ section.id }}">
          {%- if p.featured_image -%}
            {{ p.featured_image | image_url: width: 1200 | image_tag: loading: 'eager', id: 'flexMainImg', class: 'flex-main-img', widths: '200,300,400,500,600,800,1000,1200', sizes: '(max-width: 768px) 100vw, 50vw' }}
          {%- else -%}
            <div style="display:flex;align-items:center;justify-content:center;height:100%;color:#8A8279">No image</div>
          {%- endif -%}
        </div>
        <button class="flex-gallery-nav-{{ section.id }} flex-gallery-nav-prev-{{ section.id }}" id="flexPrev-{{ section.id }}" aria-label="Previous image">&#8249;</button>
        <button class="flex-gallery-nav-{{ section.id }} flex-gallery-nav-next-{{ section.id }}" id="flexNext-{{ section.id }}" aria-label="Next image">&#8250;</button>
      </div>
      {%- if p.images.size > 1 -%}
        <div class="flex-gallery-thumbs-{{ section.id }}" id="flexThumbs-{{ section.id }}">
          {%- for img in p.images -%}
            <div class="flex-gallery-thumb-{{ section.id }}{% if forloop.first %} active{% endif %}" data-image="{{ img | image_url: width: 1200 }}" data-index="{{ forloop.index0 }}">
              {{ img | image_url: width: 300 | image_tag: loading: 'lazy', class: 'flex-thumb-img' }}
            </div>
          {%- endfor -%}
        </div>
      {%- endif -%}
    </div>

    <div class="flex-info-{{ section.id }}">
      <div class="flex-info-brand-{{ section.id }}">SENSOL FITNESS</div>
      <h1 class="flex-info-title-{{ section.id }}">{{ p.title | default: 'Sensol FLEX' }}</h1>
      <div class="flex-info-subtitle-{{ section.id }}">{{ section.settings.subtitle | default: 'Grow Stronger, Train Smarter' }}</div>
      
      <!-- Judge.me Review Stars -->
      <div class="flex-info-reviews-{{ section.id }}">
        <span class="flex-info-stars-{{ section.id }}">★★★★★</span>
        <span>4.9 (112 reviews)</span>
      </div>
      
      <div class="flex-info-model-{{ section.id }}">{{ section.settings.model_info | default: 'Model FLEX / Smart Reformer · SKU: SNSL-FLEX-AU' }}</div>

      <div class="flex-info-price-{{ section.id }}">
        {%- if p.compare_at_price > p.price -%}
          <s>{{ p.compare_at_price | money }}</s>
        {%- endif -%}
        {{ p.price | money }} AUD
      </div>
      <div class="flex-info-afterpay-{{ section.id }}">or 4 interest-free payments of {{ p.price | divided_by: 4 | money }} with Afterpay</div>

      <div class="flex-info-badges-{{ section.id }}">
        <span>LIFETIME WARRANTY</span>
        <span>FREE DOMESTIC SHIPPING</span>
        <span>30-DAY MONEY-BACK</span>
      </div>

      {%- if p.available -%}
        <div class="flex-info-stock-{{ section.id }} flex-info-stock-in-{{ section.id }}">In Stock</div>
      {%- else -%}
        <div class="flex-info-stock-{{ section.id }} flex-info-stock-out-{{ section.id }}">Sold Out</div>
      {%- endif -%}

      {%- form 'product', p, id: 'product-form-' -%}
        <input type="hidden" name="id" value="{{ p.selected_or_first_available_variant.id }}" class="product-variant-id">
        <div class="subscriptions_app_embed_block"></div>
        <div class="flex-qty-{{ section.id }}">
          <label class="flex-qty-label-{{ section.id }}" for="Quantity-{{ section.id }}">Quantity</label>
          <input class="flex-qty-input-{{ section.id }}" type="number" id="Quantity-{{ section.id }}" name="quantity" value="1" min="1">
        </div>

        <button class="flex-add-{{ section.id }}" type="submit" name="add" {%- unless p.available -%}disabled{%- endunless -%}>
          {%- if p.available -%}Add to Cart{%- else -%}Sold Out{%- endif -%}
        </button>
        
        <!-- Dynamic Payment Buttons -->
        <div class="flex-payment-buttons-{{ section.id }}">
          {{ form | payment_button }}
        </div>
      {%- endform -%}

      <ul class="flex-features-{{ section.id }}">
        <li>Dual 1000W servo motor · 90 kg digital resistance</li>
        <li>0–132° adjustable arm range · 8 positions · 200+ movements</li>
        <li>Three training modes: Standard, Off-centre, Constant</li>
        <li>Free Vesta Sensol App — 300+ classes, no subscription</li>
        <li>Dual-function: Pilates + strength training in one machine</li>
      </ul>
    </div>
  </div>
</div>

<!-- Lightbox -->
<div class="flex-lightbox-{{ section.id }}" id="flexLightbox-{{ section.id }}">
  <button class="flex-lightbox-close-{{ section.id }}" id="flexLightboxClose-{{ section.id }}">&times;</button>
  <button class="flex-lightbox-nav-{{ section.id }} flex-lightbox-nav-prev-{{ section.id }}" id="flexLightboxPrev-{{ section.id }}" aria-label="Previous">&#8249;</button>
  <img src="" alt="Product image" id="flexLightboxImg-{{ section.id }}">
  <button class="flex-lightbox-nav-{{ section.id }} flex-lightbox-nav-next-{{ section.id }}" id="flexLightboxNext-{{ section.id }}" aria-label="Next">&#8250;</button>
</div>

<script>
(function() {
  var sectionId = '{{ section.id }}';
  var mainImg = document.getElementById('flexMainImg');
  var mainContainer = document.getElementById('flexMainImage-' + sectionId);
  var thumbs = document.querySelectorAll('.flex-gallery-thumb-' + sectionId);
  var prevBtn = document.getElementById('flexPrev-' + sectionId);
  var nextBtn = document.getElementById('flexNext-' + sectionId);
  var lightbox = document.getElementById('flexLightbox-' + sectionId);
  var lightboxImg = document.getElementById('flexLightboxImg-' + sectionId);
  var lightboxClose = document.getElementById('flexLightboxClose-' + sectionId);
  var lightboxPrev = document.getElementById('flexLightboxPrev-' + sectionId);
  var lightboxNext = document.getElementById('flexLightboxNext-' + sectionId);

  var images = [];
  thumbs.forEach(function(thumb) {
    images.push(thumb.getAttribute('data-image'));
  });
  if (images.length === 0 && mainImg) {
    images.push(mainImg.src);
  }
  var currentIndex = 0;

  function showImage(index) {
    if (images.length === 0) return;
    if (index < 0) index = images.length - 1;
    if (index >= images.length) index = 0;
    currentIndex = index;
    if (mainImg) {
      mainImg.src = images[index];
      mainImg.removeAttribute('srcset');
      mainImg.removeAttribute('sizes');
    }
    thumbs.forEach(function(t, i) { t.classList.toggle('active', i === index); });
    // Auto-scroll thumbs to keep active one visible
    if (thumbs[currentIndex] && thumbs[currentIndex].scrollIntoView) {
      thumbs[currentIndex].scrollIntoView({ behavior: 'smooth', block: 'nearest', inline: 'nearest' });
    }
  }

  thumbs.forEach(function(thumb, i) {
    thumb.addEventListener('click', function() { showImage(i); });
  });
  if (prevBtn) {
    prevBtn.addEventListener('click', function(e) { e.stopPropagation(); showImage(currentIndex - 1); });
  }
  if (nextBtn) {
    nextBtn.addEventListener('click', function(e) { e.stopPropagation(); showImage(currentIndex + 1); });
  }

  if (mainContainer) {
    mainContainer.addEventListener('click', function() {
      if (lightboxImg && images[currentIndex]) { lightboxImg.src = images[currentIndex]; }
      lightbox.classList.add('active');
      document.body.style.overflow = 'hidden';
    });
  }

  function closeLightbox() {
    lightbox.classList.remove('active');
    document.body.style.overflow = '';
  }
  if (lightboxClose) { lightboxClose.addEventListener('click', closeLightbox); }
  if (lightbox) {
    lightbox.addEventListener('click', function(e) { if (e.target === lightbox) closeLightbox(); });
  }
  if (lightboxPrev) {
    lightboxPrev.addEventListener('click', function(e) {
      e.stopPropagation();
      var ni = (currentIndex - 1 + images.length) % images.length;
      currentIndex = ni; lightboxImg.src = images[ni];
    });
  }
  if (lightboxNext) {
    lightboxNext.addEventListener('click', function(e) {
      e.stopPropagation();
      var ni = (currentIndex + 1) % images.length;
      currentIndex = ni; lightboxImg.src = images[ni];
    });
  }
  document.addEventListener('keydown', function(e) {
    if (!lightbox.classList.contains('active')) return;
    if (e.key === 'Escape') closeLightbox();
    if (e.key === 'ArrowLeft') {
      var ni = (currentIndex - 1 + images.length) % images.length;
      currentIndex = ni; lightboxImg.src = images[ni];
    }
    if (e.key === 'ArrowRight') {
      var ni = (currentIndex + 1) % images.length;
      currentIndex = ni; lightboxImg.src = images[ni];
    }
  });
})();
</script>

{% schema %}
{
  "name": "Sensol FLEX Landing",
  "tag": "section",
  "class": "flex-landing-section",
  "settings": [
    {
      "type": "header",
      "content": "Product Landing Section"
    },
    {
      "type": "text",
      "id": "subtitle",
      "label": "Subtitle",
      "default": "Grow Stronger, Train Smarter"
    },
    {
      "type": "text",
      "id": "model_info",
      "label": "Model Info",
      "default": "Model FLEX / Smart Reformer · SKU: SNSL-FLEX-AU"
    },
    {
      "type": "range",
      "id": "padding_top",
      "min": 0,
      "max": 200,
      "step": 10,
      "unit": "px",
      "label": "Top padding",
      "default": 140
    },
    {
      "type": "range",
      "id": "padding_bottom",
      "min": 0,
      "max": 200,
      "step": 10,
      "unit": "px",
      "label": "Bottom padding",
      "default": 80
    }
  ],
  "presets": [
    {
      "name": "Sensol FLEX Landing"
    }
  ]
}
{% endschema %}
